# ── Confident AI Default Preset (Local) ──
# Used when no remote evaluation is needed.
evaluation:
  local: true
  remote: false
  metric_collection: npc-dataset-quality
tracing:
  enabled: true
  sample_rate: 1.0
metadata:
  environment: development
