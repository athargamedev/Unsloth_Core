# Copy to .env and fill locally. Do not commit real keys.
CONFIDENT_API_KEY=
CONFIDENT_WS_BASE_URL=wss://deepeval.confident-ai.com/ws/relay

# Future local endpoint targets for AI Connections / Agent routing notes.
UCORE_NPC_SINGLE_ENDPOINT=http://host.docker.internal:8765/eval/single
UCORE_NPC_CONVERSATION_ENDPOINT=http://host.docker.internal:8765/eval/conversation
