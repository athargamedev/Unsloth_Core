# Project-Local Skill Notes

This directory is reserved for Unsloth_Core-specific skill notes or wrappers.

Current recommended skills:

- `compact-memory-extraction` — extract durable memories safely.
- `unsloth-context-audit` — audit project/context before complex operations.
- `unsloth-audit-implementation` — implementation pattern for audit CLI work.
- `diagnose-empty-tensorboard-panel` — dashboard metrics panel debugging.
- `review-dashboard-tab-ux` — dashboard tab UX review.
- `supabase-container-recovery` — local Supabase container recovery.
- `deepeval` — DeepEval eval workflow.

When a new repeated Unsloth_Core procedure is learned, create a project-scoped skill with the `skill` tool instead of writing a long procedure here.
